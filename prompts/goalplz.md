---
description: Turn a rough request into a Codex Goal mode objective and run it when appropriate
argument-hint: REQUEST...
---

Use $goalplz to turn the following request into a clear Codex goal and run it when appropriate.

If the request is not suitable for Goal mode, say so briefly and proceed normally unless the user explicitly asked to force Goal mode.

If the request is empty, ask the user for the rough request to goalplz.

Request:

$ARGUMENTS
